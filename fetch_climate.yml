name: Fetch Climate Data

on:
  schedule:
    # Midnight Lisbon winter time (UTC+0) = 00:00 UTC
    - cron: "0 0 * * *"
    # Midnight Lisbon summer time (UTC+1) = 23:00 UTC previous day
    - cron: "0 23 * * *"
  workflow_dispatch:  # allows manual run from GitHub UI

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install openpyxl requests

      - name: Run climate fetch script
        run: python fetch_climate.py

      - name: Commit and push updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/climate_guimaraes.xlsx
          git diff --staged --quiet || git commit -m "climate data: $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
